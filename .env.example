# === AWS Settings ===
AWS_S3_BUCKET=your-bucket-name

# === Flags ===
AWS_UPLOAD=true        # Upload cleaned data and report to S3
CLEAN_OUTPUT=false     # Delete generated PNGs, PDF, CSV after run
